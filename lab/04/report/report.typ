#import "@local/bubble-sysu:0.1.0": *

#show: report.with(
  title: "实验四：电影评论情感分类",
  subtitle: "大数据原理与技术实验报告",
  student: (name: "元朗曦", id: "23336294"),
  school: "计算机学院",
  major: "计算机科学与技术",
  class: "计八",
)

= 实验目的

本实验围绕电影评论情感分类任务，使用 IMDB 影评数据集判断一段英文评论表达的是正面情感还是负面情感。实验的核心目标有三个：第一，完成文本数据的读取、清洗和标签组织；第二，使用 TF-IDF 将非结构化文本转换为可供传统机器学习模型使用的数值特征；第三，分别训练逻辑回归、线性支持向量机和循环神经网络模型，并比较它们在准确率和运行时间上的差异。

该任务是典型的文本二分类问题。与图像、表格等结构化数据不同，文本长度不固定，词语顺序和词频都可能携带情感信息。因此，实验既使用强调词频统计的 TF-IDF 特征，也使用能够按序列读取词语的 RNN 模型，观察两类方法在相同数据集上的表现差异。

= 数据集与预处理

实验使用公开 IMDB 电影评论数据集 aclImdb。数据集将评论按训练集和测试集划分，每个划分中又包含 `pos` 与 `neg` 两类文本文件。其中 `pos` 表示正面评论，标签记为 1；`neg` 表示负面评论，标签记为 0。实验代码会在 `data/raw/aclImdb` 不存在时自动下载并解压数据集，然后将文本和标签整理为 `data/processed/train.csv` 与 `data/processed/test.csv`，便于重复运行。

预处理阶段主要完成三项工作。首先，程序读取原始 `.txt` 文件并去除 HTML 换行标记与多余空白。其次，训练集和测试集分别按正负样本合并，并使用固定随机种子打乱顺序，避免训练时连续读到同一类别。最后，传统模型直接使用清洗后的完整句子输入 TF-IDF 向量器；RNN 模型则先用正则表达式切分英文单词，再根据训练集构建词表，将每条评论截断或补齐为固定长度的整数序列。

= 方法设计

== TF-IDF 特征

TF-IDF 用词频和逆文档频率共同衡量一个词对文本的区分能力。若某个词在一篇评论中频繁出现，但在整个语料库中并不常见，则它可能更能代表该评论的情感倾向。实验中使用 `TfidfVectorizer` 提取 unigram 和 bigram 特征，并限制最大特征数，避免过高维度带来过大的训练开销。向量器还启用了英文停用词过滤和 sublinear TF，使常见词的影响更加平滑。

== 逻辑回归

逻辑回归是线性二分类模型。它学习每个 TF-IDF 特征对应的权重，并通过 sigmoid 函数输出样本属于正面类别的概率。对于 IMDB 这种词语线索明显、训练样本较多的文本分类任务，逻辑回归通常可以作为稳定的基线方法。它的优点是训练速度较快、结果容易解释，缺点是主要依赖词袋特征，不能直接建模远距离词序关系。

== 线性 SVM

支持向量机通过寻找最大间隔分类超平面实现分类。文本 TF-IDF 特征通常是高维稀疏向量，线性 SVM 在这类数据上表现较好。实验中使用 `LinearSVC`，它不显式计算概率，而是直接学习一个线性决策边界。与逻辑回归相比，SVM 的间隔最大化目标可能在高维文本空间中获得更强的分类边界。

== GRU RNN

循环神经网络将评论看成词语序列，而不是仅看成词频集合。实验使用轻量 GRU 模型：首先用 Embedding 层把词 ID 转换为低维向量，然后用 GRU 按顺序读取词向量，最后将最后一个隐藏状态输入全连接层进行二分类。为了保证课程作业环境可运行，模型的词表规模、评论最大长度、隐藏层维度和训练轮数都设置得较小。因此，RNN 更适合用来观察序列模型的基本效果，而不是追求深度学习模型的最高精度。

= 运行方式

实验代码位于 `lab/04` 目录。建议先在仓库根目录创建并启用虚拟环境：

```bash
python3 -m venv .venv
source .venv/bin/activate
```

进入实验目录后安装依赖：

```bash
cd lab/04
python -m pip install -r requirements.txt
```

然后运行完整实验：

```bash
./run.sh
```

首次运行时，脚本会自动下载 IMDB 数据集并生成缓存文件。完整数据集训练 RNN 可能耗时较长；如果只想快速检查流程，可以临时设置环境变量，例如：

```bash
LAB04_MAX_TRAIN=2000 LAB04_MAX_TEST=1000 LAB04_RNN_EPOCHS=1 ./run.sh
```

实验结束后，分类指标会写入 `report/result.csv`，准确率对比图和训练/测试耗时图会写入 `report/figures/`。若要生成 PDF 报告，可以进入 `report` 目录执行：

```bash
typst compile report.typ
```

= 实验结果

#let result = csv("result.csv")

#figure(
  text(size: 8pt)[
    #table(
      columns: result.at(0).len(),
      table.header(..result.at(0).map(it => strong(it))),
      ..result.slice(1).flatten(),
    )
  ],
  caption: [不同模型在 IMDB 测试集上的分类结果],
)

#figure(
  image("figures/accuracy_comparison.svg", width: 90%),
  caption: [三种模型的准确率对比],
)

#figure(
  image("figures/time_comparison.svg", width: 90%),
  caption: [三种模型的训练时间和测试时间对比],
)

从结果表和图中可以比较三类模型的差异。逻辑回归和线性 SVM 都使用 TF-IDF 特征，因此它们的性能主要取决于词语和短语的统计区分度。电影评论中常见的情感词，例如表示喜欢、失望、精彩、糟糕的词语，通常可以被 TF-IDF 较好捕捉，因此传统模型即使结构简单，也能得到较稳定的准确率。

线性 SVM 与逻辑回归的差别主要来自优化目标。逻辑回归关注类别概率估计，SVM 更关注分类间隔。在高维稀疏文本向量中，SVM 往往能形成较清晰的分割边界，因此如果实验结果中 SVM 准确率略高，这是符合预期的；如果两者接近，也说明 IMDB 情感分类中 TF-IDF 特征本身已经提供了较强信号。

GRU RNN 的特点是能够按顺序处理文本，理论上可以利用局部词序和上下文关系。但本实验为了保证运行时间，只使用较小的词向量、隐藏层和训练轮数，同时没有引入预训练词向量或复杂注意力机制。因此，RNN 的训练时间通常明显长于传统模型，而准确率不一定超过 TF-IDF + 线性分类器。这说明深度学习模型并不是只要结构更复杂就必然更好，还需要足够训练轮数、模型容量和调参过程支撑。

= 结论

本实验完成了 IMDB 电影评论情感二分类流程，包括数据自动下载、文本清洗、TF-IDF 特征提取、逻辑回归训练、线性 SVM 训练和 GRU RNN 训练。实验输出统一写入 CSV，并通过 SVG 图表展示准确率与运行时间，方便复现实验和比较模型。

从方法特点看，TF-IDF + 逻辑回归可以作为简单稳定的基线；TF-IDF + 线性 SVM 适合高维稀疏文本特征，通常能够取得较好的分类边界；GRU RNN 能表达词序信息，但在轻量配置下训练成本更高，准确率优势不一定明显。对于本作业的电影评论分类任务，传统 TF-IDF 方法已经能够充分利用词语级情感线索，而 RNN 更适合作为进一步扩展深度文本分类方法的起点。
