#import "@local/bubble-sysu:0.1.0": *

#show: report.with(
  title: "图像边缘检测与分类",
  subtitle: "大数据原理与技术实验报告",
  student: (name: "元朗曦", id: "23336294"),
  school: "计算机学院",
  major: "计算机科学与技术",
  class: "计八",
)

= 实验目的

本实验使用 Python 与 OpenCV 实现图像边缘检测，并通过机器学习方法完成图像分类任务。实验分为两个部分：第一部分选择一张图像，使用 Sobel 算子提取边缘信息；第二部分使用手写数字图像数据集，分别训练支持向量机（SVM）和卷积神经网络（CNN），并从准确率和计算效率两个角度比较两类方法。

= Sobel 边缘检测

Sobel 算子通过两个方向的离散卷积核估计图像灰度变化。水平方向梯度主要响应垂直边缘，垂直方向梯度主要响应水平边缘。设图像灰度矩阵为 $I$，Sobel 算子分别计算 $G_x$ 与 $G_y$，再将两个方向的响应合成为边缘强度图。

本实验脚本会在缺少示例图片时自动生成一张包含矩形、圆形、直线和文字的测试图像。随后将图像转为灰度图，并调用 OpenCV 的 `cv2.Sobel` 完成边缘检测。

#figure(
  image("figures/original.png", width: 70%),
  caption: [原始图像],
)

#figure(
  image("figures/gray.png", width: 70%),
  caption: [灰度图像],
)

#figure(
  image("figures/sobel.png", width: 70%),
  caption: [Sobel 边缘检测结果],
)

= 图像分类方法

== 支持向量机

SVM 是一种经典监督学习方法。本实验将每张图像展平成一维特征向量，并使用 RBF 核函数进行分类。SVM 的优点是结构清晰、在中小规模数据集上表现稳定；缺点是训练复杂度随样本规模增加较快，并且没有显式利用图像的二维空间结构。

== 卷积神经网络

CNN 通过卷积层提取局部空间特征，再通过池化层降低特征尺寸，最后使用全连接层完成分类。与 SVM 相比，CNN 能直接利用图像的二维结构，因此更适合图像分类任务。本实验使用两层卷积和两层池化构建轻量级 CNN，以控制训练时间。

= 实验结果

#let result = csv("result.csv")

#figure(
  table(
    columns: 5,
    table.header(..result.at(0).flatten().map(it => strong(it))),
    ..result.slice(1).flatten(),
  ),
  caption: [SVM 与 CNN 分类结果对比],
)

从结果可以看出，SVM 与 CNN 都能完成手写数字图像分类。SVM 将图像直接视为向量，训练过程相对直接；CNN 通过卷积和池化提取图像局部特征，通常在图像分类任务上具有更强的表达能力。实际准确率和耗时会受到数据集规模、CPU/GPU 环境、训练轮数等因素影响。

= 效率分析

本实验记录的训练时间表示模型从开始训练到训练完成的耗时，测试时间表示模型在测试集上完成预测的耗时。SVM 的训练效率受样本数量影响明显；CNN 训练时需要多轮参数更新，训练时间通常更长，但在有 GPU 的环境中可以获得更好的加速效果。

在本实验设置下，`report/result.csv` 中保存了两种方法的 `accuracy`、`train_time_sec` 和 `test_time_sec`。这些数据可以直接用于比较两种方法在准确率和计算效率上的差异。

= 结论

Sobel 算子能够有效突出图像中灰度变化明显的位置，适合作为基础边缘检测方法。SVM 和 CNN 都可以用于图像分类，但两者关注点不同：SVM 更依赖人工设计或展平后的特征表示，CNN 则通过卷积结构自动学习空间特征。综合准确率和计算效率，CNN 更适合图像任务的进一步扩展，而 SVM 适合作为传统机器学习基线方法。
