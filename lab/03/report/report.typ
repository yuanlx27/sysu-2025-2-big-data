#import "@local/bubble-sysu:0.1.0": *

#show: report.with(
  title: "实验三：聚类算法对比实验",
  subtitle: "大数据原理与技术实验报告",
  student: (name: "元朗曦", id: "23336294"),
  school: "计算机学院",
  major: "计算机科学与技术",
  class: "计八",
)

= 实验目的

本实验在鸢尾花数据集上实现并比较 K-means 和 DBSCAN 两种聚类算法。实验重点不是直接调用现成聚类接口，而是手写两种算法的核心流程，并通过调整簇数、邻域半径和最小邻居数观察聚类结果变化。最后使用准确率、轮廓系数和 Calinski-Harabasz 指数从外部类别一致性和内部聚类结构两个角度评价算法效果。

= 数据集与评价方法

实验使用 `sklearn.datasets.load_iris` 提供的内置 Iris 数据集。数据集包含 150 个样本，每个样本有花萼长度、花萼宽度、花瓣长度和花瓣宽度四个数值特征，真实类别包括 setosa、versicolor 和 virginica 三类。由于不同特征量纲不同，实验在聚类前对每个特征进行标准化处理，使其均值为 0、标准差为 1。

聚类算法本身不使用真实标签。为了计算准确率，实验在聚类完成后将每个簇映射为该簇中出现次数最多的真实类别，并统计映射后的预测类别与真实类别的一致比例。DBSCAN 中被标记为噪声的样本不参与簇到类别的多数投票映射，在准确率中按未正确分类处理。

轮廓系数衡量样本与本簇和最近其他簇之间的相对距离，取值越高通常说明簇内更紧密、簇间更分离。Calinski-Harabasz 指数比较簇间离散程度和簇内离散程度，数值越高通常表示聚类结构越清晰。对于 DBSCAN，内部指标只在非噪声样本上计算；如果有效簇数不足两个，则记为 `N/A`。

= 算法实现

== K-means

K-means 假设数据可以被划分为给定数量的簇。实验实现过程包括四步：第一，随机选择 $k$ 个样本作为初始中心；第二，计算每个样本到各中心的欧氏距离，并分配到最近中心；第三，用簇内样本均值更新中心；第四，当中心移动距离小于阈值或达到最大迭代次数时停止。若某次迭代中出现空簇，则使用距离现有中心最远的样本作为该簇的新中心，避免算法中断。

本实验分别设置 $k=2$、$k=3$ 和 $k=4$。其中 $k=3$ 与 Iris 数据集真实类别数一致，通常应获得较好的类别一致性；$k=2$ 会合并部分类别，$k=4$ 则可能将原本连续分布的类别进一步拆分。

== DBSCAN

DBSCAN 通过密度连接关系发现簇，不需要预先指定簇数。实验实现中，若某样本在半径 `eps` 内的邻居数不少于 `min_samples`，则该样本为核心点；从核心点出发可以不断扩展密度可达的邻居，形成一个簇；无法归入任何簇的样本被标记为噪声。

本实验固定 `min_samples=5`，分别设置 `eps=0.4`、`eps=0.6` 和 `eps=0.8`。较小的 `eps` 会使邻域更严格，噪声点更容易增多；较大的 `eps` 会连接更多样本，可能减少噪声，但也可能把原本不同的簇合并。

= 运行实验

实验代码位于 `lab/03` 目录。为了避免系统 Python 环境缺少依赖或与其他课程实验相互影响，运行前建议先在仓库根目录创建并启用虚拟环境：

```bash
python3 -m venv .venv
source .venv/bin/activate
```

进入实验目录后，使用该虚拟环境安装本实验所需依赖：

```bash
cd lab/03
python -m pip install -r requirements.txt
```

依赖安装完成后，运行主脚本：

```bash
./run.sh
```

`run.sh` 会进入实验目录，创建 `report/figures` 文件夹，并调用 `src/experiment.py` 完成完整实验流程。该脚本首先读取 scikit-learn 内置鸢尾花数据集并进行标准化，然后依次运行三组 K-means 参数和三组 DBSCAN 参数，最后计算准确率、轮廓系数和 Calinski-Harabasz 指数。

之后运行：

```bash
deactivate
```

退出虚拟环境。

实验完成后，评价指标会写入 `report/metrics.csv`，聚类可视化和指标对比图会写入 `report/figures/`。报告中的结果表由 `metrics.csv` 直接生成，图片也直接引用该目录下的 SVG 文件，因此重新运行脚本后报告内容会随实验结果同步更新。若需要生成 PDF 报告，可以进入 `report` 目录后执行：

```bash
typst compile report.typ
```

= 实验结果

#let result = csv("metrics.csv")

#figure(
  text(size: 7pt)[
    #table(
      columns: result.at(0).len(),
      table.header(..result.at(0).map(it => strong(it))),
      ..result.slice(1).flatten(),
    )
  ],
  caption: [不同参数下的聚类评价指标],
)

#figure(
  image("figures/metrics_comparison.svg", width: 100%),
  caption: [准确率、轮廓系数和 Calinski-Harabasz 指数对比],
)

从指标表可以观察到，K-means 的表现与簇数设置直接相关。当 $k=2$ 时，算法只能形成两个簇，因此会把两个真实类别中的样本合并到同一簇，准确率受到限制；当 $k=3$ 时，簇数与真实类别数一致，通常能得到更好的准确率和较高的 Calinski-Harabasz 指数；当 $k=4$ 时，类别可能被过度拆分，准确率和内部结构指标不一定继续提升。

DBSCAN 的结果对 `eps` 更敏感。`eps` 较小时，算法倾向于只保留密度较高的区域作为簇，边界样本可能被视为噪声；随着 `eps` 增大，噪声点减少，簇的连接范围扩大。如果 `eps` 过大，不同类别之间的边界会被弱化，聚类结果可能合并为更少的簇。

= 可视化分析

#figure(
  image("figures/kmeans_k3.svg", width: 80%),
  caption: [K-means 在 $k=3$ 时的二维 PCA 聚类结果],
)

#figure(
  image("figures/dbscan_eps_0_6_min_5.svg", width: 80%),
  caption: [DBSCAN 在 `eps=0.6`、`min_samples=5` 时的二维 PCA 聚类结果],
)

为了便于展示，实验使用 PCA 将标准化后的四维特征投影到二维空间。K-means 的聚类边界更接近基于中心点距离的划分，适合近似球形、规模相对均衡的簇；DBSCAN 更关注局部密度，对噪声点和非球形结构更有表达能力，但在 Iris 数据集这种类别之间存在连续过渡的数据上，参数选择会明显影响最终簇数。

= 结论

本实验完成了 K-means 与 DBSCAN 的核心逻辑实现，并在鸢尾花数据集上比较了多组参数。K-means 的主要超参数是簇数 $k$，当 $k$ 接近真实结构时效果较好，但需要预先指定簇数；DBSCAN 通过 `eps` 和 `min_samples` 控制密度定义，不需要指定簇数，并能识别噪声点，但对邻域半径较敏感。

从实验结果看，两种算法适用场景不同。K-means 实现简单、运行稳定，适合作为基础聚类方法；DBSCAN 对簇形状和噪声更灵活，但需要结合数据尺度仔细调整参数。对于 Iris 数据集，标准化和参数选择都会显著影响最终聚类表现，因此在实际应用中应同时观察评价指标和可视化结果。
